name: CI

on:
  push:
  pull_request:

jobs:
  m1-host:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: M1 qualification
        run: make clean qualify-m1
